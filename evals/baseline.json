{
  "tier1_min": 1.0,
  "_note": "Minimum acceptable Tier-1 corpus score. The deterministic hook must classify every fixture correctly; any drop is a discipline-signal regression (drift). Raise this only alongside adding harder fixtures — never lower it to make a red build green without justifying the regression."
}
